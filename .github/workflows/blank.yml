name: CI

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Echo when released
        if: github.event.prerelease
        run: echo "Package was prelereased"
        
      - name: Echo when released
        if: ${{ !github.event.prerelease }}
        run: echo "Package was released"
